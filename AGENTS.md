# AGENTS

## Cursor Cloud specific instructions

This is currently a blank repository (BLBTST) with no source code, dependencies, or services.

- **No build/lint/test commands** are available yet — the repo contains only a `README.md`.
- **No update script dependencies** — when code is added, update this section and the VM update script accordingly.
- Future agents should check for newly added `package.json`, `requirements.txt`, `pyproject.toml`, or similar dependency files and install as needed.
