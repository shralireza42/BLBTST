"use client"; import { PageShell } from "../../components/layout/PageShell"; import { Card } from "../../components/ui/Card"; import { Button } from "../../components/ui/Button"; import { StatCard } from "../../components/ui/StatCard"; import { useReferralStats } from "../../lib/hooks/useBlobbyApi"; export default function Referrals(){const q=useReferralStats(); const d:any=q.data||{}; return <PageShell eyebrow="Referrals" title="Invite the blob mob" intro="Share your code, track conversions, and earn pending rewards."><div className="grid gap-4 md:grid-cols-4"><StatCard label="Code" value={d.code||'--'}/><StatCard label="Active" value={d.active||0}/><StatCard label="Converted" value={d.converted||0}/><StatCard label="Earned" value={d.earned||'--'}/></div><Card className="mt-6"><h2 className="display-text text-3xl">My referral link</h2><p className="mt-3 break-all rounded-2xl bg-white p-4 border-2 border-[#020202]">https://itsblobbie.com/ref/{d.code||'BLOBBIE'}</p><Button className="mt-4">Copy</Button></Card></PageShell>}
