import { AdminShell } from "../../../components/admin/AdminShell"; import { AdminTable } from "../../../components/admin/AdminTable"; import { Card } from "../../../components/ui/Card";
export default function AdminPage(){return <AdminShell title="Contract sync status"><div className="grid gap-6"><AdminTable title="Overview" rows={['DailyDraw cursor','Jackpot cursor','Confirmation depth','Last processed event']}/><Card><h2 className="display-text text-2xl">Protected API</h2><p>This page calls protected backend APIs with RBAC. Store admin API key only in local browser storage for local operations; secrets are never embedded in code.</p></Card></div></AdminShell>}
