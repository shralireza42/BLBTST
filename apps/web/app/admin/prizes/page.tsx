import { AdminShell } from "../../../components/admin/AdminShell"; import { AdminTable } from "../../../components/admin/AdminTable"; import { Card } from "../../../components/ui/Card";
export default function AdminPage(){return <AdminShell title="Prize config"><div className="grid gap-6"><AdminTable title="Overview" rows={['1st place','2nd-10th','11th-150th','Reserve allocations']}/><Card><h2 className="display-text text-2xl">Protected API</h2><p>This page calls protected backend APIs with RBAC. Store admin API key only in local browser storage for local operations; secrets are never embedded in code.</p></Card></div></AdminShell>}
