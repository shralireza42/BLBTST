import { PageShell } from "../../components/layout/PageShell"; import { JackpotStatusCard } from "../../components/jackpot/JackpotStatusCard"; import { JackpotHistory } from "../../components/jackpot/JackpotHistory"; import { Card } from "../../components/ui/Card"; import { Button } from "../../components/ui/Button"; export default function JackpotPage(){return <PageShell eyebrow="Jackpot" title="Reserve. Threshold. One winner." intro="Every eligible paid ticket can become a jackpot entry after the previous payout cycle."><div className="grid gap-6 lg:grid-cols-2"><JackpotStatusCard/><Card><h2 className="display-text text-3xl">Audit details</h2><p className="mt-2">Cycle start timestamp, threshold, VRF request data, randomness proof, payout details, and explorer links are exposed through public verification.</p><Button href="/verify" className="mt-4">Verify jackpot</Button></Card><JackpotHistory/><Card><h3 className="display-text text-2xl">Randomness proof data</h3><p>VRF request and payout transaction data appear after cycle settlement.</p></Card></div></PageShell>}
