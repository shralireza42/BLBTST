{
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @blobby/web build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
