import { Container } from "./Container"; export function PageShell({ eyebrow, title, intro, children }: { eyebrow?: string; title: string; intro?: string; children: React.ReactNode }) { return <main className="pt-28 pb-16"><Container><div className="mb-8 max-w-4xl">{eyebrow&&<p className="mb-2 text-sm uppercase tracking-[.25em]">{eyebrow}</p>}<h1 className="display-text text-4xl sm:text-6xl lg:text-7xl">{title}</h1>{intro&&<p className="mt-4 max-w-2xl text-lg sm:text-xl">{intro}</p>}</div>{children}</Container></main>; }
