"use client"; import { Card } from "../ui/Card"; import { Badge } from "../ui/Badge"; import { Progress } from "../ui/Progress"; import { Countdown } from "../ui/Countdown"; export function DrawStatusCard({ draw }: { draw:any }) { const round=draw?.chain||draw?.round||draw; const count=Number(round?.eligibleTicketCount||0); return <Card><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="display-text text-3xl">Current Round</h2><Badge tone="green">{String(round?.status??'OPEN')}</Badge></div><div className="mt-4 grid gap-4 sm:grid-cols-3"><div><p>Countdown</p><p className="display-text text-2xl"><Countdown target={round?.expiresAt}/></p></div><div><p>Tickets</p><p className="display-text text-2xl">{count}/300</p></div><div><p>Unique wallets</p><p className="display-text text-2xl">{String(round?.uniqueWalletCount??'--')}</p></div></div><div className="mt-4"><Progress value={count} max={300}/></div></Card> }
